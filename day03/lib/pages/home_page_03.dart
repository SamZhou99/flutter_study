import 'package:flutter/material.dart';

class HomePage03 extends StatelessWidget {
  const HomePage03({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("HomePage03", style: TextStyle(fontSize: 36))),
    );
  }
}
